	Сделал установку (обновление) модуля.
	Теперь нужно следующее:
	обновить все скрипты .install, чтобы принимали (и обрабатывали) параметр currentVersion.
	Глянуть в документацию (а есть ли там что-то вообще про модули?)
Обновить скрипт отката установки (не удаления!), чтобы обрабатывал событие currentVersion.
Сделать отдельные файлы: install.mysql.sql, install.sqlite.sql, mysql.sql, sqlite.sql
